#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/jx" ]; then
  "$basedir/jx"  "$basedir/../nodemon/bin/nodemon.js" "$@"
  ret=$?
else 
  jx  "$basedir/../nodemon/bin/nodemon.js" "$@"
  ret=$?
fi
exit $ret
