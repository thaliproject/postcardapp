#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/jx" ]; then
  "$basedir/jx"  "$basedir/../nopt/bin/nopt.js" "$@"
  ret=$?
else 
  jx  "$basedir/../nopt/bin/nopt.js" "$@"
  ret=$?
fi
exit $ret
