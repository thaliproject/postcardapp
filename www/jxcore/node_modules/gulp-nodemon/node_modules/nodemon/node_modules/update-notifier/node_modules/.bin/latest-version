#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/jx" ]; then
  "$basedir/jx"  "$basedir/../latest-version/cli.js" "$@"
  ret=$?
else 
  jx  "$basedir/../latest-version/cli.js" "$@"
  ret=$?
fi
exit $ret
