#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/jx" ]; then
  "$basedir/jx"  "$basedir/../js-yaml/bin/js-yaml.js" "$@"
  ret=$?
else 
  jx  "$basedir/../js-yaml/bin/js-yaml.js" "$@"
  ret=$?
fi
exit $ret
