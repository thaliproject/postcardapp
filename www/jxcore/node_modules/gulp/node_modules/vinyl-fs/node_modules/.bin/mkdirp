#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/jx" ]; then
  "$basedir/jx"  "$basedir/../mkdirp/bin/cmd.js" "$@"
  ret=$?
else 
  jx  "$basedir/../mkdirp/bin/cmd.js" "$@"
  ret=$?
fi
exit $ret
